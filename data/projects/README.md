# Projects

This directory contains a Markdown file per project. Each file uses YAML
front-matter for structured metadata (title, category, stack, repo, status)
followed by a long-form description.

To add a new project, create a new `.md` file here and (optionally) add a
matching card to `pages/projects.html`.
