# Experiences

This directory contains a Markdown file per professional experience.
Each file uses YAML front-matter for structured metadata (role, company,
period, location) followed by responsibilities and skills applied.

To add a new experience, create a new `.md` file here and (optionally) add
a matching card to `pages/experience.html`.
